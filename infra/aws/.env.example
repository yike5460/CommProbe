# This file is for reference only. 
# Reddit API credentials should be passed via CDK context parameters.

# Example deployment command:
# npx cdk deploy --context redditClientId=YOUR_CLIENT_ID --context redditClientSecret=YOUR_SECRET

# Optional: You can also set these in cdk.json under "context" section:
# {
#   "context": {
#     "redditClientId": "your_client_id",
#     "redditClientSecret": "your_client_secret",
#     "redditUserAgent": "legal-crawler/1.0"
#   }
# }

# AWS Configuration (uses default profile if not set)
AWS_REGION=us-east-1
CDK_DEFAULT_REGION=us-east-1