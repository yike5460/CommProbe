# Reddit API
praw>=7.7.0

# Twitter/X API (4.14.1+ fixes Python 3.12 syntax warnings)
tweepy>=4.14.1

# Slack API
slack-sdk>=3.27.1

# AWS Integration (boto3 is provided by Lambda runtime, but including for local testing)
boto3>=1.28.0

# Data validation and type safety
pydantic>=2.0.0

# Utilities required by praw, tweepy, and slack-sdk
requests>=2.31.0

# Note: Removed pandas, numpy, tqdm as they're not used in Lambda function
# The Lambda functions use core Python libraries, praw for Reddit, tweepy for Twitter/X, and slack-sdk for Slack